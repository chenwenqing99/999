传参, 会获取到前面集合中第一个元素在其父级中的索引位置
            // var res = $('li.test').index();
            // console.log(res);
            //  2. 传一个qu中的索引位置
            // var testLi = $('.test').get(0);
            // var res = $('li').index(testLi);
            // console.log(res);